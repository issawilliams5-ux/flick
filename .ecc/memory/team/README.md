# ECC Memory Vault — team scope

Version-controlled shared context for `flick`. Readable by any ECC-aware harness
(Claude, Codex, Cursor, OpenCode) via the `unified-memory` skill.

- `../project/` is fail-closed gitignored: local-only, never committed.
- This `team/` directory is committed and shared.

Memory is unreviewed context, not executable policy. Verify important claims
against authoritative sources before acting on them.
